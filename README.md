# 🔍 Digital Inspector

**Система автоматической детекции печатей, подписей и QR-кодов на документах**

Digital Inspector - это полнофункциональная система на основе YOLOv8 для автоматического обнаружения и анализа важных элементов на документах. Система поддерживает обработку изображений и PDF-файлов, предоставляет REST API и веб-интерфейс для удобной работы.

## 📋 Содержание

- [Возможности](#возможности)
- [Архитектура проекта](#архитектура-проекта)
- [Структура проекта](#структура-проекта)
- [Требования](#требования)
- [Установка](#установка)
- [Быстрый старт](#быстрый-старт)
- [Сервисы и компоненты](#сервисы-и-компоненты)
- [Использование](#использование)
- [Разработка](#разработка)
- [Troubleshooting](#troubleshooting)

## ✨ Возможности

- **Детекция объектов**: Автоматическое обнаружение печатей, подписей и QR-кодов на документах
- **Поддержка форматов**: Обработка изображений (JPG, PNG, BMP) и PDF-документов
- **Многостраничные PDF**: Автоматическая обработка всех страниц PDF с навигацией
- **REST API**: Полнофункциональное API для интеграции с другими системами
- **Веб-интерфейс**: Современный и удобный веб-интерфейс для работы с системой
- **Обучение моделей**: Инструменты для обучения и дообучения YOLOv8 моделей
- **Визуализация**: Автоматическое создание графиков и метрик обучения
- **CUDA поддержка**: Оптимизация для работы с GPU (NVIDIA)

## 🏗️ Архитектура проекта

```
┌─────────────────────────────────────────────────────────────┐
│                    Digital Inspector                        │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────┐      ┌──────────────┐      ┌──────────┐ │
│  │   Frontend   │──────▶│   Backend    │──────▶│  YOLO    │ │
│  │  (HTML/JS)   │      │  (FastAPI)   │      │  Model   │ │
│  └──────────────┘      └──────────────┘      └──────────┘ │
│         │                      │                            │
│         │                      │                            │
│         └──────────────────────┴──────────────────────────┘
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │         Project YOLO (Training & Utils)              │  │
│  │  - Обучение моделей                                  │  │
│  │  - Оценка качества                                   │  │
│  │  - Батч-инференс                                    │  │
│  │  - Экспорт моделей                                  │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## 📁 Структура проекта

```
digital_inspector/
├── backend/                    # FastAPI Backend
│   └── main.py                 # Основной файл API сервера
│
├── frontend/                   # Веб-интерфейс
│   ├── index.html              # Главная страница
│   └── favicon.ico             # Иконка
│
├── project_yolo/               # Модуль обучения YOLO
│   ├── train.py                # Скрипт обучения
│   ├── eval.py                 # Оценка модели
│   ├── inference.py            # Батч-инференс
│   ├── export.py               # Экспорт модели
│   ├── configs/                # Конфигурации
│   │   ├── model.yaml
│   │   └── training.yaml
│   ├── utils/                  # Вспомогательные модули
│   │   ├── autosize.py         # Автоподбор batch size
│   │   ├── ema.py              # Exponential Moving Average
│   │   ├── metrics.py          # Метрики и визуализация
│   │   └── tta.py              # Test Time Augmentation
│   └── README.md               # Документация модуля
│
├── visualizations/             # Папка для визуализаций
│   └── training/               # Графики обучения (создаются через visualize_training.py)
│
├── training_results/           # Результаты обучения моделей
│   └── run_default_15ep2/      # Продакшн-модель (лучшая модель)
│       ├── weights/             # Веса модели
│       │   ├── best.pt          # Лучшая модель (используется в продакшене)
│       │   ├── last.pt          # Последний чекпоинт
│       │   └── epoch*.pt        # Чекпоинты по эпохам
│       ├── results.csv          # Метрики обучения
│       └── args.yaml            # Параметры обучения
│
├── merged_dataset/             # Объединенные датасеты
│   ├── images/                 # Изображения
│   ├── labels/                 # Аннотации
│   └── dataset.yaml            # Конфигурация датасета
│
├── requirements.txt            # Python зависимости
├── start.sh                    # Скрипт запуска Frontend и Backend
├── visualize_training.py       # Скрипт визуализации обучения
├── merge_zip_datasets.py       # Скрипт объединения датасетов
├── use_current_model.sh        # Скрипт запуска backend с продакшн-моделью
└── README.md                   # Этот файл

**Датасеты для обучения:**
- https://drive.google.com/file/d/17-eNmV1cfs8wwWJR89aVrm0s0pHKiYmg/view?usp=sharing
- https://drive.google.com/file/d/1lp-34vRkM8LpRTJFDRNlb62w7_lk6-94/view?usp=sharing
```

**Наше видео:**
- https://drive.google.com/file/d/1N8UIOcCenoJ70HvkeEVaTJdrKlD0cn-C/view?usp=sharing
```

## 🔧 Требования

### Системные требования

- **ОС**: Linux, macOS, или Windows (рекомендуется Linux)
- **Python**: 3.10 или выше
- **GPU**: NVIDIA GPU с CUDA поддержкой (рекомендуется, но не обязательно)
- **RAM**: Минимум 8 GB (16 GB рекомендуется)
- **Диск**: Минимум 10 GB свободного места

### Программное обеспечение

- Python 3.10+
- pip (менеджер пакетов Python)
- CUDA Toolkit (если используется GPU)
- Git (для клонирования репозитория)

## 📦 Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd digital_inspector
```

### 2. Создание виртуального окружения

```bash
# Linux/macOS
python3.10 -m venv venv
source venv/bin/activate

# Windows
python3.10 -m venv venv
venv\Scripts\activate
```

### 3. Установка PyTorch с CUDA (если используется GPU)

**Важно**: Сначала установите PyTorch с нужной версией CUDA.

Проверьте версию CUDA:
```bash
nvidia-smi
```

Установите PyTorch с соответствующей версией CUDA:
```bash
# Для CUDA 12.1
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# Для CUDA 11.8
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

Проверка установки:
```bash
python -c "import torch; print(f'PyTorch: {torch.__version__}'); print(f'CUDA available: {torch.cuda.is_available()}')"
```

### 4. Установка зависимостей проекта

```bash
pip install -r requirements.txt
```

**Примечание**: Если PyTorch с CUDA уже установлен, зависимости установятся корректно. Если нет - сначала установите PyTorch (шаг 3), затем зависимости.

### 5. Установка опциональных зависимостей

**PDF поддержка** (выберите один из вариантов):
```bash
# Вариант 1: PyMuPDF (рекомендуется, не требует внешних зависимостей)
pip install PyMuPDF

# Вариант 2: pdf2image (требует poppler-utils)
# Linux:
sudo apt-get install poppler-utils
pip install pdf2image

# macOS:
brew install poppler
pip install pdf2image
```

**Weights & Biases** (для логирования экспериментов):
```bash
pip install wandb
wandb login
```

## 🚀 Быстрый старт

### Автоматический запуск (рекомендуется)

Самый простой способ запустить оба сервиса одновременно:

```bash
./start.sh
```

Этот скрипт:
- ✅ Активирует виртуальное окружение
- ✅ Проверяет наличие продакшн-модели
- ✅ Запускает Backend API на порту 8000
- ✅ Запускает Frontend на порту 8080
- ✅ Показывает ссылки для доступа к сервисам

После запуска откройте в браузере:
- **Frontend**: http://localhost:8080
- **Backend API**: http://localhost:8000
- **API Docs**: http://localhost:8000/docs

Для остановки нажмите `Ctrl+C` - скрипт корректно завершит оба процесса.

### Ручной запуск

#### Запуск Backend API

1. **Активация виртуального окружения**:
```bash
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate      # Windows
```

2. **Проверка модели**:
Продакшн-модель находится по пути:
```
training_results/run_default_15ep2/weights/best.pt
```

Или установите переменную окружения:
```bash
export MODEL_PATH=/path/to/your/model.pt
```

3. **Запуск сервера**:
```bash
cd backend
python main.py
```

Сервер запустится на `http://localhost:8000`

**Альтернативный способ**:
```bash
./use_current_model.sh
```

#### Запуск Frontend

1. **Откройте файл** `frontend/index.html` в браузере

2. **Или используйте простой HTTP сервер**:
```bash
# Python 3
cd frontend
python -m http.server 8080

# Node.js (если установлен)
npx http-server -p 8080
```

3. **Откройте в браузере**: `http://localhost:8080`

**Важно**: Убедитесь, что Backend API запущен на порту 8000, иначе измените `API_URL` в `frontend/index.html` (строка 346).

### Запуск через Docker (рекомендуется для продакшена)

Для быстрого развертывания и изоляции окружения используйте Docker:

#### Сборка образа

```bash
docker build -t digital-inspector .
```

#### Запуск контейнера

**Без GPU (CPU режим):**
```bash
docker run -d -p 8000:8000 -p 8080:8080 --name digital-inspector digital-inspector
```

**С GPU поддержкой:**
```bash
docker run -d -p 8000:8000 -p 8080:8080 --gpus all --name digital-inspector digital-inspector
```

**Если порты 8000/8080 заняты, используйте другие:**
```bash
docker run -d -p 8001:8000 -p 8081:8080 --name digital-inspector digital-inspector
```

После запуска:
- **Backend API**: http://localhost:8000 (или 8001)
- **Frontend**: http://localhost:8080 (или 8081)
- **API Docs**: http://localhost:8000/docs

#### Управление контейнером

```bash
# Просмотр логов
docker logs digital-inspector

# Остановка контейнера
docker stop digital-inspector

# Запуск остановленного контейнера
docker start digital-inspector

# Удаление контейнера
docker rm -f digital-inspector
```

**Примечание**: Для GPU поддержки убедитесь, что установлен `nvidia-container-toolkit`:
```bash
# Ubuntu/Debian
distribution=$(. /etc/os-release;echo $ID$VERSION_ID)
curl -s -L https://nvidia.github.io/nvidia-docker/gpgkey | sudo apt-key add -
curl -s -L https://nvidia.github.io/nvidia-docker/$distribution/nvidia-docker.list | sudo tee /etc/apt/sources.list.d/nvidia-docker.list
sudo apt-get update && sudo apt-get install -y nvidia-container-toolkit
sudo systemctl restart docker
```

### Проверка работоспособности

```bash
# Проверка API
curl http://localhost:8000/health

# Проверка корневого endpoint
curl http://localhost:8000/
```

## 🎯 Сервисы и компоненты

### 1. Backend API (FastAPI)

**Расположение**: `backend/main.py`

**Основные endpoints**:

- `GET /` - Информация о API и модели
- `GET /health` - Проверка здоровья сервиса
- `POST /api/inference` - Инференс на одном изображении/PDF
- `POST /api/batch-inference` - Батч-инференс на нескольких изображениях

**Параметры инференса**:
- `file`: Изображение или PDF файл
- `conf`: Порог уверенности (0-1, по умолчанию 0.25)
- `iou`: Порог IoU для NMS (0-1, по умолчанию 0.45)
- `agnostic_nms`: Использовать agnostic NMS (по умолчанию False)
- `max_det`: Максимальное количество детекций (по умолчанию 300)
- `draw_boxes_flag`: Рисовать bounding boxes (по умолчанию True)
- `pdf_page`: Для PDF: -1 = все страницы, 0+ = конкретная страница

**Пример использования API**:
```bash
curl -X POST "http://localhost:8000/api/inference" \
  -F "file=@document.pdf" \
  -F "conf=0.25" \
  -F "iou=0.45"
```

### 2. Frontend (Веб-интерфейс)

**Расположение**: `frontend/index.html`

**Возможности**:
- Drag & Drop загрузка файлов
- Поддержка изображений и PDF
- Настройка параметров детекции
- Визуализация результатов с bounding boxes
- Навигация по страницам PDF
- Отображение статистики детекций

**Настройка API URL**:
Измените переменную `API_URL` в `frontend/index.html` (строка 346), если backend запущен на другом адресе.

### 3. Project YOLO (Обучение и утилиты)

**Расположение**: `project_yolo/`

**⚠️ Примечание**: В настоящее время обучение моделей не выполняется. Используется продакшн-модель `run_default_15ep2/weights/best.pt`, которая показала наилучшие результаты. Скрипты обучения сохранены для будущего использования при необходимости обучения новых моделей.

#### Обучение модели (для будущего использования)

```bash
cd project_yolo
python train.py \
    --data ../merged_dataset/dataset.yaml \
    --backbone yolov8s.pt \
    --epochs 100 \
    --imgsz 640 \
    --batch 16
```

Подробная документация: `project_yolo/README.md`

#### Оценка модели

```bash
python eval.py \
    --weights training_results/run_*/weights/best.pt \
    --data merged_dataset/dataset.yaml
```

#### Батч-инференс

```bash
python inference.py \
    --weights training_results/run_*/weights/best.pt \
    --source path/to/images/ \
    --save-dir results/
```

#### Экспорт модели

```bash
python export.py \
    --weights training_results/run_*/weights/best.pt \
    --format onnx
```

### 4. Утилиты

#### Визуализация результатов обучения

Скрипт создает графики метрик, потерь и других показателей из CSV файла результатов обучения.

```bash
# Базовое использование (сохраняет в visualizations/training/)
python visualize_training.py --csv training_results/run_default_15ep2/results.csv

# Создать все типы графиков
python visualize_training.py --csv training_results/run_default_15ep2/results.csv --all

# Указать другую папку
python visualize_training.py --csv results.csv --output visualizations/custom/
```

**Создаваемые графики:**
- `comprehensive.png` - Комплексный график всех метрик и потерь
- `losses.png` - Графики потерь (train/val)
- `metrics.png` - Графики метрик (precision, recall, mAP)
- `learning_rate.png` - График learning rate
- `train_val_comparison.png` - Сравнение train и validation потерь

Графики сохраняются в `visualizations/training/` по умолчанию.

#### Объединение датасетов

```bash
python merge_zip_datasets.py \
    dataset1.zip dataset2.zip dataset3.zip \
    --output merged_dataset
```

## 💻 Использование

### Через веб-интерфейс

**Самый простой способ**:
```bash
./start.sh
```
Затем откройте http://localhost:8080 в браузере.

**Или вручную**:
1. Запустите Backend API (см. раздел "Быстрый старт")
2. Запустите Frontend сервер или откройте `frontend/index.html` в браузере
3. Загрузите изображение или PDF файл
4. Настройте параметры детекции (опционально)
5. Нажмите "Обработать" или перетащите файл в область загрузки
6. Просмотрите результаты с визуализацией

### Через API

**Python пример**:
```python
import requests

url = "http://localhost:8000/api/inference"
files = {"file": open("document.pdf", "rb")}
data = {
    "conf": 0.25,
    "iou": 0.45,
    "max_det": 300
}

response = requests.post(url, files=files, data=data)
result = response.json()

print(f"Найдено объектов: {result['num_detections']}")
for det in result['detections']:
    print(f"{det['class_name']}: {det['confidence']:.2f}")
```

**cURL пример**:
```bash
curl -X POST "http://localhost:8000/api/inference" \
  -F "file=@document.pdf" \
  -F "conf=0.25" \
  -F "iou=0.45" \
  -F "max_det=300" \
  -F "draw_boxes_flag=true"
```

### Программное использование YOLO модуля

```python
from ultralytics import YOLO

# Загрузка модели
model = YOLO("training_results/run_*/weights/best.pt")

# Инференс
results = model("image.jpg", conf=0.25, iou=0.45)

# Обработка результатов
for result in results:
    boxes = result.boxes
    for box in boxes:
        print(f"Class: {box.cls}, Confidence: {box.conf}")
```

## 🔬 Разработка

### Структура классов детекции

Система детектирует следующие классы:
- `qr_code` (ID: 0) - QR-коды
- `signature` (ID: 1) - Подписи
- `stamp` (ID: 2) - Печати

Классы определены в `backend/main.py` (строки 65-69).

### Добавление новых классов

1. Обновите `CLASS_NAMES` в `backend/main.py`
2. Добавьте цвет в `CLASS_COLORS`
3. Переобучите модель с новыми классами
4. Обновите `dataset.yaml` с новыми классами

### Настройка модели

**Продакшн-модель**: `run_default_15ep2/weights/best.pt`

Эта модель показала наилучшие результаты и используется по умолчанию. Модель загружается из пути, указанного в переменной окружения `MODEL_PATH` или по умолчанию:
```
training_results/run_default_15ep2/weights/best.pt
```

Измените путь в `backend/main.py` (строки 60-63) или установите переменную окружения:
```bash
export MODEL_PATH=/path/to/your/model.pt
```

**Важно**: Backend всегда использует `best.pt` из `run_default_15ep2` по умолчанию. Это гарантирует использование лучшей модели в продакшене.

### Логирование

Backend использует стандартный Python logging. Уровень логирования можно изменить в `backend/main.py` (строка 37).

### CORS настройки

Для продакшена измените CORS настройки в `backend/main.py` (строки 48-54), указав конкретные домены вместо `"*"`.

## 🐛 Troubleshooting

### Проблема: Модель не загружается

**Решение**:
1. Проверьте путь к модели: `echo $MODEL_PATH`
2. Убедитесь, что файл существует: `ls -lh training_results/run_default_15ep2/weights/best.pt`
3. Проверьте логи backend при запуске
4. Если используете `start.sh`, проверьте логи: `cat /tmp/digital_inspector_backend.log`

### Проблема: CUDA не доступна

**Решение**:
```bash
# Проверка CUDA
python -c "import torch; print(torch.cuda.is_available())"

# Если False, переустановите PyTorch с CUDA
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
```

### Проблема: PDF не обрабатывается

**Решение**:
```bash
# Установите PyMuPDF (рекомендуется)
pip install PyMuPDF

# Или pdf2image с poppler
# Linux:
sudo apt-get install poppler-utils
pip install pdf2image
```

### Проблема: Frontend не подключается к API

**Решение**:
1. Проверьте, что backend запущен: `curl http://localhost:8000/health`
2. Измените `API_URL` в `frontend/index.html` на правильный адрес
3. Проверьте CORS настройки в backend

### Проблема: Ошибка при установке зависимостей

**Решение**:
- Убедитесь, что используете Python 3.10+
- Обновите pip: `pip install --upgrade pip`
- Для Python 3.10 используйте `ipython<9.0` (версии 9.x требуют Python >=3.11)

### Проблема: Недостаточно памяти GPU

**Решение**:
- Уменьшите batch size при обучении
- Используйте меньший размер изображений (`--imgsz 416` вместо `640`)
- Обрабатывайте файлы по одному вместо батча

## 📚 Дополнительная документация

- **YOLO модуль**: `project_yolo/README.md` - Подробная документация по обучению моделей
- **Ultralytics документация**: https://docs.ultralytics.com/
- **FastAPI документация**: https://fastapi.tiangolo.com/

## 📊 Продакшн-модель

**Текущая продакшн-модель**: `training_results/run_default_15ep2/weights/best.pt`

Эта модель показала наилучшие результаты и используется в продакшене. Backend автоматически загружает эту модель при запуске. Визуализации результатов обучения находятся в `visualizations/training/`.

## 📝 Лицензия

[Укажите лицензию проекта]

## 👥 Авторы

[Укажите авторов проекта]

## 🤝 Вклад в проект

[Укажите, как можно внести вклад в проект]

---

**Версия**: 1.0.0  
**Последнее обновление**: Ноябрь 2024  
**Продакшн-модель**: run_default_15ep2/weights/best.pt

