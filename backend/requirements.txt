fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic>=2.0.0
# torch и torchvision уже установлены в основном venv с CUDA
# ultralytics тоже уже установлен
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0
# PDF поддержка (рекомендуется PyMuPDF - не требует внешних зависимостей):
PyMuPDF>=1.23.0
# Альтернатива: pdf2image>=1.16.0  # Требует poppler-utils (apt-get install poppler-utils)

