# ============================================================================
# Backend API Dependencies (FastAPI, Uvicorn, Pydantic)
# Примечание: fastapi, uvicorn, python-multipart, pydantic могут быть добавлены отдельно
# ============================================================================
# fastapi>=0.104.0
# uvicorn[standard]>=0.24.0
# python-multipart>=0.0.6
# pydantic>=2.0.0
#
# ============================================================================
# PDF Support
# ============================================================================
# PDF поддержка (рекомендуется PyMuPDF - не требует внешних зависимостей):
# PyMuPDF>=1.23.0
# Альтернатива: pdf2image>=1.16.0  # Требует poppler-utils (apt-get install poppler-utils)
#
# ============================================================================
# Optional Dependencies
# ============================================================================
# Optional: GPU monitoring
# pynvml>=11.5.0
#
# Optional: Experiment tracking
# wandb>=0.15.0
#
# Optional: TensorRT (install separately if needed)
# tensorrt>=8.6.0
# pycuda>=2022.2.2
#
# ============================================================================
# Core Python Dependencies
# ============================================================================
absl-py==2.3.0
aiohappyeyeballs==2.6.1
aiohttp==3.12.4
aiosignal==1.3.2
argon2-cffi==23.1.0
argon2-cffi-bindings==21.2.0
arrow==1.3.0
asttokens==3.0.0
astunparse==1.6.3
async-lru==2.0.5
attrs==25.3.0
babel==2.17.0
bleach==6.2.0
certifi==2025.4.26
cffi==1.17.1
charset-normalizer==3.4.2
comm==0.2.2
contourpy==1.3.2
cycler==0.12.1
debugpy==1.8.14
decorator==5.2.1
defusedxml==0.7.1
einops==0.8.1
executing==2.2.0
fastjsonschema==2.21.1
filelock==3.18.0
flatbuffers==25.9.23
fonttools==4.58.1
fqdn==1.5.1
frozenlist==1.6.0
fsspec==2025.5.1
gast==0.6.0
google-pasta==0.2.0
grpcio==1.72.1
h11==0.16.0
h5py==3.15.1
httpcore==1.0.9
httpx==0.28.1
# ICD mappings - не используется в проекте
# icd-mappings==0.4.0
idna==3.10
# ImageIO - не используется напрямую в коде (используется PIL/OpenCV)
# imageio==2.37.0
importlib-resources==5.13.0
# Jupyter/IPython - не используются напрямую в коде (только для разработки)
# ipykernel==6.29.5
# IPython: версии 9.x требуют Python >=3.11
# Для Python 3.10 используйте: ipython<9.0 или ipython==8.37.0
# ipython==8.37.0
# ipython_pygments_lexers==1.1.1
isoduration==20.11.0
# JAX - не используется напрямую в проекте
# jax==0.6.2
# jaxlib==0.6.2
jedi==0.19.2
Jinja2==3.1.6
joblib==1.5.1
json5==0.12.0
jsonpointer==3.0.0
jsonschema==4.24.0
jsonschema-specifications==2025.4.1
# Jupyter packages - не используются напрямую в коде (только для разработки)
# jupyter-events==0.12.0
# jupyter-lsp==2.2.5
# jupyter_client==8.6.3
# jupyter_core==5.8.1
# jupyter_server==2.16.0
# jupyter_server_terminals==0.5.3
# jupyterlab==4.4.3
# jupyterlab_pygments==0.3.0
# jupyterlab_server==2.27.3
# Keras - не используется напрямую (зависимость TensorFlow)
# keras==3.11.3
kiwisolver==1.4.8
lazy_loader==0.4
libclang==18.1.1
# PyTorch Lightning - не используется напрямую в коде
# lightning==2.5.1.post0
# lightning-utilities==0.14.3
llvmlite==0.44.0
Markdown==3.8
markdown-it-py==4.0.0
MarkupSafe==3.0.2
# Visualization
matplotlib==3.10.3
matplotlib-inline==0.1.7
mdurl==0.1.2
# MediaPipe - не используется напрямую в коде
# mediapipe==0.10.21
mistune==3.1.3
ml_dtypes==0.5.3
mpmath==1.3.0
multidict==6.4.4
namex==0.1.0
nbclient==0.10.2
nbconvert==7.16.6
nbformat==5.10.4
nest-asyncio==1.6.0
networkx==3.4.2
notebook==7.4.3
notebook_shim==0.2.4
numba==0.61.2
# Image processing and numerical computing
numpy==1.26.4
# NVIDIA CUDA packages are automatically installed as dependencies of PyTorch/TensorFlow
# when installed with CUDA support. Do not pin them here to avoid version conflicts.
# Image processing libraries
opencv-contrib-python==4.11.0.86
opencv-python==4.11.0.86
opt_einsum==3.4.0
optree==0.17.0
overrides==7.7.0
packaging==25.0
# Data handling
pandas==2.2.3
pandocfilters==1.5.1
parso==0.8.4
# Image processing
pillow==11.2.1
platformdirs==4.3.8
# Polars - не используется напрямую в коде (используется pandas)
# polars==1.35.2
# polars-runtime-32==1.35.2
prometheus_client==0.22.0
prompt_toolkit==3.0.51
propcache==0.3.1
protobuf==6.33.0
psutil==7.1.3
pure_eval==0.2.3
pycparser==2.22
Pygments==2.19.2
pyparsing==3.2.3
python-dateutil==2.9.0.post0
python-json-logger==3.3.0
# PyTorch Lightning - не используется напрямую в коде
# pytorch-lightning==2.5.1.post0
pytz==2025.2
# Data handling
PyYAML==6.0.2
pyzmq==26.4.0
referencing==0.36.2
requests==2.32.3
# Audio processing - не используется в проекте
# resampy==0.4.3
rfc3339-validator==0.1.4
rfc3986-validator==0.1.1
rich==14.2.0
rpds-py==0.25.1
# Scikit packages - не используются напрямую в коде
# scikit-image==0.25.2
# Machine learning and data science
# scikit-learn==1.6.1
scipy==1.15.3
# Visualization
seaborn==0.13.2
Send2Trash==1.8.3
# SentencePiece - не используется напрямую в коде
# sentencepiece==0.2.1
six==1.17.0
# Audio processing - не используется в проекте
# sounddevice==0.5.3
# soundfile==0.13.1
stack-data==0.6.3
# SymPy - не используется напрямую в коде
# sympy==1.14.0
# TensorFlow и TensorBoard - не используются напрямую в коде
# Могут быть нужны только для экспорта в TensorFlow формат через ultralytics
# tensorboard==2.20.0
# tensorboard-data-server==0.7.2
# tensorboardX==2.6.2.2
# tensorflow==2.20.0
# tensorflow-io-gcs-filesystem==0.37.1
termcolor==3.1.0
terminado==0.18.1
threadpoolctl==3.6.0
# Tifffile - не используется напрямую в коде
# tifffile==2025.5.26
tinycss2==1.4.0
# PyTorch: torch и torchvision уже установлены в основном venv с CUDA
# НЕ устанавливайте их через pip install -r requirements.txt, если уже установлены с CUDA!
# Для установки с CUDA используйте: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# torch==2.7.0
# torchmetrics==1.7.2
# torchvision==0.22.0
tornado==6.5.1
tqdm==4.67.1
traitlets==5.14.3
# Triton - устанавливается вместе с PyTorch
# triton==3.3.0
types-python-dateutil==2.9.0.20250516
typing_extensions>=4.15.0
tzdata==2025.2
# YOLO object detection (уже установлен в основном venv с CUDA)
ultralytics==8.3.228
ultralytics-thop==2.0.18
uri-template==1.3.0
urllib3==2.4.0
wcwidth==0.2.13
webcolors==24.11.1
webencodings==0.5.1
websocket-client==1.8.0
Werkzeug==3.1.3
# WFDB (Waveform Database) - не используется в проекте
# wfdb==4.3.0
wrapt==2.0.0
yarl==1.20.0
