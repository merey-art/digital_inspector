# 🚀 Быстрый старт Digital Inspector

## Запуск веб-приложения

### 1. Установка зависимостей backend

**Важно:** Используйте то же виртуальное окружение, где установлен torch с CUDA.

```bash
# Активируйте основное venv
source venv/bin/activate

# Установите только недостающие зависимости
cd backend
pip install -r requirements.txt
```

**Примечание:** `torch`, `torchvision` и `ultralytics` уже установлены в основном venv и не будут переустановлены.

### 2. Запуск backend (в одном терминале)

```bash
# Вариант 1: Через скрипт
./start_backend.sh

# Вариант 2: Вручную
cd backend
source ../venv/bin/activate
python main.py
```

Backend будет доступен на `http://localhost:8000`

### 3. Запуск frontend (в другом терминале)

```bash
# Вариант 1: Через скрипт
./start_frontend.sh

# Вариант 2: Вручную
cd frontend
python3 -m http.server 8080
```

Затем откройте в браузере: `http://localhost:8080`

## Использование

1. Откройте `http://localhost:8080` в браузере
2. Загрузите изображение документа (drag & drop или кнопка)
3. Настройте параметры детекции (опционально):
   - Порог уверенности (conf)
   - Порог IoU
   - Максимальное количество детекций
   - Подавление overlapping детекций
4. Получите результаты с визуализацией

## API Документация

После запуска backend:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

## Проверка здоровья API

```bash
curl http://localhost:8000/health
```

## Пример использования API напрямую

```bash
curl -X POST "http://localhost:8000/api/inference" \
  -F "file=@merged_dataset/images/val/your_image.jpg" \
  -F "conf=0.25" \
  -F "iou=0.45"
```

## Настройка модели

По умолчанию используется: `training_results/run_doc_v12/weights/best.pt`

Чтобы использовать другую модель:
```bash
export MODEL_PATH=/path/to/your/model.pt
python backend/main.py
```

## Структура проекта

```
digital-inspector/
├── backend/              # FastAPI backend
│   ├── main.py          # API сервер
│   └── requirements.txt
├── frontend/            # Веб-интерфейс
│   └── index.html      # HTML/JS приложение
├── project_yolo/        # YOLO тренировка
└── training_results/    # Обученные модели
```

## Решение проблем

### Модель не найдена
Убедитесь, что файл `training_results/run_doc_v12/weights/best.pt` существует, или установите `MODEL_PATH`.

### CORS ошибки
Если frontend на другом порту, обновите CORS в `backend/main.py`.

### Медленная обработка
- Проверьте, что CUDA доступна: `curl http://localhost:8000/health`
- Уменьшите размер изображений
- Уменьшите параметр `max_det`

